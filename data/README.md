# `data/` 디렉터리

봇이 동작하려면 **`ehcmall_index.json`** 이 이 폴더에 있어야 합니다.

- 카탈로그를 만드는 쪽(내부)에서 ETL로 생성한 뒤, **파일만** 안전한 경로로 전달받아 두세요.
- `ehcmall.ttl` 은 API가 읽지 않지만, 감사·백업용으로 같이 두어도 됩니다.
- GitHub에 **민감도·용량** 때문에 올리지 않는 것을 권장합니다. `.gitignore`에 `*.json` / `*.ttl` 이 포함되어 있습니다.
