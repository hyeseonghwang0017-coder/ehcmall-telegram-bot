# API는 기본적으로 서버시작.command / uvicorn 실행 시
# INDEX_PATH=./data/ehcmall_index.json 으로 경로가 잡힙니다.
# 다른 경로를 쓸 때만 설정하세요.
# INDEX_PATH=/absolute/path/to/ehcmall_index.json

# 텔레그램 봇 토큰은 OpenClaw 설정(~/.openclaw/…)에 둡니다.
# 이 폴더의 API는 토큰을 읽지 않습니다.
